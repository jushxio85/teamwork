## 系統環境圖 (DFD)

```mermaid
flowchart LR
    %% === 外部實體 ===
    User([使用者])
    Notif([通知服務])
    Auth([登入驗證系統])
    DB[(系統資料庫)]

    %% === 系統邊界 ===
    subgraph System["課課到，分分算 系統"]
        direction TB
        SYS[課表與成績管理系統]
    end

    %% === 外部資料流 ===
    User -->|登入帳號 / 密碼| Auth
    Auth -->|登入驗證結果| SYS

    User -->|輸入課表 / 成績資料| SYS
    SYS -->|上課提醒 / 成績結果| User

    SYS -->|通知排程| Notif
    Notif -->|發送課程提醒| User

    SYS -->|讀取 / 儲存資料| DB

---

### 說明：
| 元件 | 說明 |
|------|------|
| **使用者 (User)** | 學生，使用系統輸入課表與成績資料 |
| **登入驗證系統 (Auth)** | 提供使用者登入帳密驗證 |
| **通知服務 (Notif)** | 提供課程提醒通知功能 |
| **系統資料庫 (DB)** | 儲存課程、成績與提醒資料 |
| **課課到，分分算 系統 (System)** | 本系統核心，負責管理課表、成績與提醒功能 |

---

這張圖是整個系統的「外部互動總覽」，
代表一個大黑盒子「課課到，分分算系統」如何與外部世界（使用者、驗證服務、通知服務、資料庫）交換資料。

---
